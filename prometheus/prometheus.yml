global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: uptime-monitor
    static_configs:
      - targets: ["monitor:8000"]
